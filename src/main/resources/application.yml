spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tour
    username: root
    password: example
    initialization-mode: always

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop # none, create, update, create-drop
    generate:-ddl: true

server:
  servlet:
    contextPath: /